extends ../layout

block content
    h1 CREATE A NEW RELEASE

    img(src="https://s1.ax1x.com/2020/05/14/YwxKYT.png", alt="效果预览图", width="700px")

    ul
        li 
            p(class="text"). 
                #[code Project ID(required)] 字段如何获取？
                打开 gitlab 进入到项目的 home 页，在项目名称的大标题下面附有 Project ID: xxx 这样的字样，取 
                #[code :] 
                后面的数字即可
        li
            p(class="text"). 
                #[code Release Name] 字段表示本次 Release 的名称
        
        li
            p(class="text"). 
                #[code Tag Name] 字段是希望根据哪个 Tag 来进行 Release 操作

        li
            p(class="text"). 
                #[code Description] 字段希望描述本次 Release 的改动
        
        li
            p(class="text"). 
                #[code Milestone] 字段表示本次 Release 希望关联到哪个里程碑当中

    form(action="/gitlab/release", method="post")
        .row
            .col-3
                label(for="project_id") Project ID: 
                input(type="text", id="project_id", name="project_id", autocomplete="off", required="true")
            .col-3
                label(for="name") Release Name: 
                input(type="text", id="name", name="name", autocomplete="off", required="true")
            .col-3
                label(for="tag_name") Tag Name: 
                input(type="text", id="tag_name", name="tag_name", autocomplete="off", required="true")

        .row
            .col-3
                label(for="description") Description: 
                input(type="text", id="description", name="description", autocomplete="off")
            .col-3
                label(for="milestone") Milestone: 
                input(type="text", id="milestone", name="milestone", autocomplete="off", required="true")
            .col-3
                label(for="assets_name") Assets Name: 
                input(type="text", id="assets_name", name="assets_name", autocomplete="off")

        .row
            .col-3
                label(for="assets_link") Assets links: 
                input(type="text", id="assets_link", name="assets_link", autocomplete="off")

        br
        button(id="submit" class="mgb-3") 🚀 CREATE RELEASE!

    script(src="javascript/gitlab.js")